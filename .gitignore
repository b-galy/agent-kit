# Local credentials — never committed. `.bg/` and `.galy/` are the folder's former names, still
# read as fallbacks.
.cs/
.bg/
.galy/

# Ephemeral content buffers pulled/pushed by the cs CLI
.tmp/

# Node
node_modules/
npm-debug.log*

# OS / editor
.DS_Store
Thumbs.db

# Codex projection — build output of scripts/build-codex.mjs, never a second copy to maintain.
.agents/
.codex/
