{
  "name": "@b-galy/agent-kit-repo",
  "version": "1.0.0",
  "description": "Castalie Agent Kit — one-command setup, runnable straight from this repository with npx.",
  "type": "module",
  "private": false,
  "bin": {
    "galy-setup": "./setup/setup.mjs"
  },
  "files": [
    "setup/setup.mjs"
  ],
  "engines": {
    "node": ">=18"
  },
  "license": "SEE LICENSE IN LICENSE",
  "scripts": {
    "build:codex": "node scripts/build-codex.mjs",
    "verify:codex": "node scripts/build-codex.mjs --verify",
    "check:codex": "node scripts/build-codex.mjs --check",
    "check:statusline": "node scripts/check-statusline.mjs",
    "check:where": "node scripts/check-where.mjs",
    "check:types": "npx -y -p typescript@5 tsc -p tsconfig.json"
  }
}
